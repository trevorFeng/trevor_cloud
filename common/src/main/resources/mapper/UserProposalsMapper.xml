<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trevor.common.dao.mysql.UserProposalsMapper">

    <!-- 新增一条记录 -->
    <insert id="insertOne">
        INSERT  INTO
        `user_proposals`(user_id ,message ,file_urls)
        VALUES (#{userProposals.userId} ,#{userProposals.message} ,#{userProposals.fileUrls})
    </insert>

</mapper>