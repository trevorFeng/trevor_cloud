server:
  port: 9003

spring:
  application:
    name: service-general

  profiles:
    include: common_dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

  instance:
    instance-id: 基本服务-9003
    prefer-ip-address: true

info:
  author.name: trevor
  app.name: general
  server.port: ${server.port}
  application.name: ${spring.application.name}
